import 'package:flutter/material.dart';

class MovieListViewTile extends StatefulWidget {
  const MovieListViewTile({ Key? key }) : super(key: key);

  @override
  _MovieListViewTileState createState() => _MovieListViewTileState();
}

class _MovieListViewTileState extends State<MovieListViewTile> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}